#!/bin/bash

#<-Variable Declaration->
self="$(basename $0)"
E_HELP=44

#<-Function Declaration->
usage () {
   echo "Usage: $self [-n|--notify] [-h|--help]"
   exit "$E_HELP"
}

fail() {
   echo "Cannot check IP address"&&exit 1
}

#<-Run Content->
case "$1" in
   -h|--help) usage;;
   *) curl -s http://icanhazip.com||fail;;
esac

exit 0
